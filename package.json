{
  "name": "nettrack-root",
  "version": "1.0.0",
  "description": "Root configuration for NetTrack MERN project",
  "scripts": {
    "dev": "concurrently \"npm run start --prefix Backend\" \"npm run dev --prefix frontend\"",
    "audit": "npm run audit --prefix Backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "jsonwebtoken": "^9.0.3"
  }
}
